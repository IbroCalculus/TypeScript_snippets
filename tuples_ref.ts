/* 
A tuple is a special type of array in TypeScript where the length and types of elements are fixed. Unlike regular arrays, tuples allow you to specify the exact structure of the array, including the type of each element. This makes tuples useful when you need to work with collections of values that have a specific order and type.
*/

let person: [string, number] = ["Alice", 25];   // The first element must be a string (e.g., "Alice"). The second element must be a number (e.g., 25).